import { QuickFlightReservation } from './quick-flight-reservation';

describe('QuickFlightReservation', () => {
  it('should create an instance', () => {
    expect(new QuickFlightReservation()).toBeTruthy();
  });
});
